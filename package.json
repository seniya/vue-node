{
  "name": "vue-node",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "cross-env-shell DEBUG=be:* NODE_ENV=development node ./be/bin/www",
    "serve": "cd fe && npm run serve",
    "pr": "cd fe && npm i && npm run build && cd ../be && npm i && cd .. && NODE_ENV=production PORT=3000 node ./be/bin/www",
    "pm2": "cd fe && npm i && npm run build && cd ../be && npm i && cd .. && pm2 start --env pr"
  },
  "dependencies": {},
  "devDependencies": {
    "cross-env": "^7.0.2"
  }
}
